services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    ports:
      - "6333:6333" # HTTP REST API
      - "6334:6334" # gRPC API (Fastest for bulk uploads)
    volumes:
      - ./qdrant_storage:/qdrant/storage
    restart: always